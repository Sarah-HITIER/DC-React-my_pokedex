import { createTheme } from "@mui/material/styles";

export const theme = createTheme({
    palette: {
        // primary: {
        //     main: "#e14718"
        // },
        background: {
            main: "#070e36"
        },
        text: {
            main: "#fff"
        }
    },
    typography: {
        fontSize: 14
    }
});
